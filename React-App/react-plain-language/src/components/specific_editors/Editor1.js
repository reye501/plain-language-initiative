import ControlledEditor from './Editor';
import '../../styles/Editor.css';

export function Editor1({ order, q_default }) {
    return(
        <ControlledEditor order={order} q_default={q_default} className="Researcher-input" id="q1"/>
    );
}

export function Editor2({ order, q_default }) {
    return(
        <ControlledEditor order={order} q_default={q_default} className="Researcher-input" id="q2"/>
    );
}

export function Editor3({ order, q_default }) {
    return(
        <ControlledEditor order={order} q_default={q_default} className="Researcher-input" id="q3"/>
    );
}

export function Editor4({ order, q_default }) {
    return(
        <ControlledEditor order={order} q_default={q_default} className="Researcher-input" id="q4"/>
    );
}

export function Editor5({ order, q_default }) {
    return(
        <ControlledEditor order={order} q_default={q_default} className="Researcher-input" id="q5"/>
    );
}

export function Editor6({ order, q_default }) {
    return(
        <ControlledEditor order={order} q_default={q_default} className="Researcher-input" id="q6"/>
    );
}